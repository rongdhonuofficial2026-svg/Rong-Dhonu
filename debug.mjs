import { createClient } from "@supabase/supabase-js"; import * as dotenv from "dotenv"; dotenv.config({ path: ".env.local" }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.from("gallery_media").select("*, exhibitions(theme_en, theme_bn, year)").order("created_at", { ascending: false }); console.log(JSON.stringify(data?.[0], null, 2)); } test();
